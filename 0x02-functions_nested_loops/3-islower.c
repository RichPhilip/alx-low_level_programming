#include "main.h"
/**
 * _islower - checks for lower case char
 * @c - the lower case char to be checked
 * Return: 1 for loweer char or 0 for anything
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	
	return (1);
	}
	return (0);


}
