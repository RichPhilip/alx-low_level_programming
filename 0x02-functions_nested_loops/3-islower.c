#include "main.h"
/**
 * _islower - checks for l88ower case  character
 * @c - the lower case char to be checked
 * Return: 1 for loweer character or 0 for anything
 */

int _islower(int c)

{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);


}
